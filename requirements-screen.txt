# Sàng lọc tự động bằng PANNs (DATA_PLAN §4.2–4.3) — scripts/auto_screen.py.
#
# TÁCH RIÊNG khỏi requirements-data.txt có chủ đích: bước chuẩn bị dữ liệu phải chạy
# được trên máy không GPU và người gán nhãn thứ hai không cần cài torch. Chỉ máy nào
# thực sự chạy sàng lọc mới cài file này.
#
# Cài thêm SAU requirements-data.txt, không thay thế nó:
#   .venv/Scripts/python.exe -m pip install -r requirements-data.txt
#   .venv/Scripts/python.exe -m pip install -r requirements-screen.txt --extra-index-url https://download.pytorch.org/whl/cu121

# torch 2.5.1 là bản cuối còn chạy với numpy 1.26 — mà numpy 1.26 là bắt buộc vì
# scaper (xem requirements-data.txt). Nâng torch mà không kiểm tra sẽ kéo numpy 2.x
# về và làm chết toàn bộ bước sinh train set.
torch==2.5.1

panns-inference==0.1.1   # CNN14 pretrain trên AudioSet, 527 lớp
torchlibrosa==0.1.0      # panns-inference phụ thuộc; ghim để tái lập được

# ⚠️ HAI DÒNG DƯỚI ĐÂY KHÔNG PHẢI THỪA — đừng xoá vì "pip tự lo được".
# panns-inference kéo theo matplotlib (nó chỉ dùng để vẽ), và matplotlib mới nhất
# đòi numpy>=2.0 → pip lặng lẽ GỠ numpy 1.26 và cài numpy 2.0.2. Scaper chết ngay
# sau đó ở np.Inf, mà không có gì nối hai sự kiện ấy với nhau: người dùng chỉ thấy
# "cài xong bộ sàng lọc thì bước sinh train set hỏng". Đã xảy ra thật 14/09/2026.
matplotlib==3.9.4        # bản cuối còn chạy với numpy 1.26
numpy==1.26.4            # lặp lại từ requirements-data.txt để pip không hạ bệ nó

# ─────────────────────────────────────────────────────────────────────────────
# Checkpoint: panns-inference tự tải Cnn14_mAP=0.431.pth (~300 MB) về ~/panns_data
# ở lần chạy đầu. KHÔNG nằm trong repo và KHÔNG có checksum do tác giả công bố —
# nếu cần tái lập tuyệt đối thì tự ghi lại SHA-256 của file đã tải.
#
# ⚠️ PANNs được huấn luyện ở 32 kHz, còn bank của ta lưu 16 kHz. auto_screen.py
# resample lên 32 kHz trước khi suy luận. Bỏ bước đó thì mô hình vẫn chạy và vẫn ra
# số, nhưng là số của một tín hiệu bị dịch cao độ một quãng tám.
